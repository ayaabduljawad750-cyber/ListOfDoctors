🏥 Doctors List - MEAN Stack Project
📌 Description

This is a simple web application built using the MEAN stack (MongoDB, Express.js, Angular, Node.js).

The project shows a list of doctors stored in a MongoDB database.
The backend provides an API, and the Angular frontend displays the data in a simple dashboard.

backend → npm install → npm start
frontend → npm install → ng serve